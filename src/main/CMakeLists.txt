add_executable(main test-scheme-convergence.cpp)
if(USE_CUDA)
  target_link_libraries(main common cuda-fct-solver cpu-fct-solver)
else()
  target_link_libraries(main common cpu-fct-solver)
endif()